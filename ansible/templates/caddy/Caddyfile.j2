@media path /media/*
@{{ app_name }} path *

{{ media_domain }} {
	file_server @media {
		hide .git
		precompressed zstd br gzip
	}
}

{{ app_domain }} {
	reverse_proxy @{{ app_name }} {
		# Reverse proxy everything. Whitenoise will serve static files.
		to /run/{{ app_name }}.sock
	}
}
